<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Parcial2_Luis_Miguel_Jerez_Caceres</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Home
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="entrada">
                <span class="oi oi-plus" aria-hidden="true"></span> Entradas
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="c_Entradas">
                <span class="oi oi-plus" aria-hidden="true"></span> Consulta
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="almacen">
                <span class="oi oi-plus" aria-hidden="true"></span> Almacen
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
